# Depth mapping using python


The current code is able to calculate depth using stereo camera and run the UI for showing the disparity image(depth) 

User interface for Desktop application is done using EEL
